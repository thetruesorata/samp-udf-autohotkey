---
title: BlockChatInput
---

Block chat input from the player. The chat can still be opened, but 
no messages or commands can be sent.

**Returns:** `true` if successful, `false` otherwise

**Signature:**

```ahk
BlockChatInput()
```

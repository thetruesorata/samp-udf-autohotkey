---
title: IsTargetInAnyVehicleByPed
---

Check whether a given PED pointer is in any vehicle.

**Parameters:**

- **dwPED**: PED pointer

**Returns:** `1` if in a vehicle, `0` if not, `-1` on error

**Signature:**

```ahk
IsTargetInAnyVehicleByPed(dwPED)
```
